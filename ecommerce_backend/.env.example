# Server
PORT=3001
HOST=0.0.0.0
NODE_ENV=development

# Database (prefer MONGODB_URI; or use MONGODB_URL + MONGODB_DB)
MONGODB_URI=mongodb://appuser:dbuser123@localhost:5001/ecommerce?authSource=admin
MONGODB_URL=
MONGODB_DB=ecommerce

# CORS
CORS_ORIGIN=http://localhost:3000

# JWT
JWT_ACCESS_SECRET=changeme_access
JWT_REFRESH_SECRET=changeme_refresh
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Optional payment providers
SITE_URL=http://localhost:3001
STRIPE_SECRET_KEY=
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# Email/SMS/WhatsApp placeholders (no keys required in dev)
